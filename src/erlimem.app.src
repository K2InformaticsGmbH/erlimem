{application, erlimem,
    [
        {description, "IMEM DB client driver"},
        {vsn, "2.0.0"},
        {applications, [
            kernel,
            ssl,
            stdlib
        ]},
        {env, [{logging, info}]},
        {mod, {erlimem, []}}
    ]
}.