{application, erlimem,
    [
        {applications, [
            kernel,
            ssl,
            stdlib
        ]},
        {description, "IMEM DB client driver"},
        {env, [{logging, info}]},
        {mod, {erlimem, []}},
        {vsn, "1.3.0"}
    ]}.
