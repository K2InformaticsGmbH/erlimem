%% -*- mode: erlang -*-
%% ex: ft=erlang
{application, erlimem,
 [
  {description, "IMEM DB client driver"},
  {vsn, "1.1.0"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  ssl
                 ]},
  {mod, { erlimem, []}},
  {env, [
         {logging, info}
        ]}
 ]}.
