%% -*- mode: erlang -*-
%% ex: ft=erlang
{application, erlimem,
 [
  {description, "IMEM DB client driver"},
  {vsn, "1.2.9"},
  {registered, []},
  {applications, [
                  kernel,
                  stdlib,
                  ssl
                 ]},
  {mod, { erlimem, []}},
  {env, [
         {logging, info}
        ]}
 ]}.
