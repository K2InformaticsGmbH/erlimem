{application, erlimem,
    [
        {description, "IMEM DB client driver"},
        {vsn, "3.0.0"},
        {applications, [
            kernel,
            ssl,
            stdlib
        ]},
        {env, [{logging, info}]},
        {mod, {erlimem, []}}
    ]
}.
