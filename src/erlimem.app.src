{application, erlimem,
    [
        {description, "IMEM DB client driver"},
        {vsn, "1.3.1"},
        {applications, [
            kernel,
            ssl,
            stdlib
        ]},
        {env, [{logging, info}]},
        {mod, {erlimem, []}}
    ]}.
